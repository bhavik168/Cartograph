Drop 5-15 plain-text documents (.txt or .md) here.

They are the only retrieval surface Cartographer has: the corpus_search tool runs
BM25 over this folder. Nothing here is committed (see .gitignore) - the corpus is
yours, not the repo's.
